import { footerStyle } from '../util/styles';

export default function Footer() {
  return (
    <>
      <footer css={footerStyle}>
        {/* TODO: darkLightMode + Copyight + (Credentials?)*/}
      </footer>
    </>
  );
}
